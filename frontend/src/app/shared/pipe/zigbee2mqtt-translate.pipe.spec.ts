import { Zigbee2mqttTranslatePipe } from './zigbee2mqtt-translate.pipe';

describe('Zigbee2mqttTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new Zigbee2mqttTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
